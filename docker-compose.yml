services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "3000:3000"
    environment:
      - DATABASE_URL=mysql://u300082146_masrat:Mohamed6231@193.203.168.53:3306/u300082146_masrat
      - NEXTAUTH_SECRET=edu-platform-nextauth-secret-2024-secure
      - JWT_SECRET=edu-platform-secret-jwt-key-2024-production-ready
      - NEXTAUTH_URL=https://masaratedu.com
      - NEXT_PUBLIC_APP_URL=https://masaratedu.com
      - NODE_ENV=production
    volumes:
      - ./uploads:/app/public/uploads
    restart: unless-stopped
